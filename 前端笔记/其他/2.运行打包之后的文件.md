```
打包：npm run build
发布1：npm install -g serve //下载serve
serve dist
访问：http://localhost:5000

发布2：使用动态web服务器(tomcat)
修改配置: webpack.prod.conf.js
output: {
	publicPath:'/xxx/' //打包文件夹的名称
}
重新打包: npm run build
修改 dist 文件夹为预目名称: xxx ，将 xxx 拷贝到运行的 tomcat 的 webapps 目录下
访问: http://localhost:8080/xxx
```

